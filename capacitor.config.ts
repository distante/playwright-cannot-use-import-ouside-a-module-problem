import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'playwright problem',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
